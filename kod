--[[ funkce main by mela byt posledni definovanou funkci ]]
function main() --Hlavni telo programu
	local a;
	local vyslakshdoiuwdh;
	
	write("Zade");
	read("*n");

	if a < 0 then
		write("Faw");
	else
		factorial(a);
		write("Vy:", vyslakshdoiuwdh, "aaa");
	end;
end;

